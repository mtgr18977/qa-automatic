QA Automation Playwright + pytest — Short README for CV

- Repository: qa-automation-playwright-py_recreated
- Purpose: Starter kit for UI (Playwright) and API (pytest + requests) automation
- Highlights:
  * Playwright (Python) UI tests: headless Chromium flows
  * API tests with pytest + requests: smoke, auth, data-driven
  * CI-ready: GitHub Actions workflow included
  * Docker support and docker-compose for isolated runs
